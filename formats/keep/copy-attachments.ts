import { TFolder } from "obsidian";

/**
 * Recreates any binary file in the Obsidian vault.
 */
async function copyFile(folderPath: TFolder, fileName: string) {
    // let { vault } = this.app; 
    // let fileAndPath = `${folderPath}/${fileName}`;
    // console.log('fileAndPath', fileAndPath);
    // await vault.createBinary(fileAndPath, await file.arrayBuffer());
}
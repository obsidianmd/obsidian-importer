import { ImportResult } from '../../main';

export const dropTheRope = async (options:Object): Promise<ImportResult> => {
    let results: ImportResult = {
		total: 0,
		failed: [],
		skipped: []
	};


    return results;
}
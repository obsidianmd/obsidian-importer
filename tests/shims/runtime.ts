/**
 * The runtime extensions, where the website and the tests share them.
 *
 * Import for side effects, as before. The implementation moved to
 * web/obsidian/runtime.ts so the browser build and this suite install the same
 * one; nothing else about it changed.
 */
import '../../web/obsidian/runtime';
